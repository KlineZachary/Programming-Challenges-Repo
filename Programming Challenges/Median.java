import java.util.*;
public class Median{
    public static void main(String[] args) {
        Scanner keyboard = new Scanner(System.in);
        // int[] numbers = {1,3,4,60,70,50,2};
        // int median = 0, medianIndex = numbers.length / 2;
        // Arrays.sort(numbers);
        // System.out.println(numbers[medianIndex]);
        PriorityQueue<Integer> numbers = new PriorityQueue<Integer>();         while(keyboard.hasNext()){
            numbers.add(keyboard.nextInt());
            // if ((numbers. % 2) == 0) {}
        }
    }
}