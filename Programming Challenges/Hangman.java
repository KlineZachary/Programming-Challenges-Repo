import java.util.*;
import java.io.*;
public class Hangman {

	public static void main(String[] args) {
        //NOT DONE 

        
        // Scanner keyboard = new Scanner(new BufferedReader(new InputStreamReader(System.in)));
        
        // StringBuilder word = new StringBuilder(keyboard.next());
        // int wordLength = word.length();
        // int correctLetter = 0;
        // int loses = 0;
        // int j = 0;
        // boolean didLose = false;
        // StringBuilder letters = new StringBuilder(keyboard.next());
        // System.out.println("WORD length :" + wordLength);
        // while((loses != 11) && (j < letters.length()) && (correctLetter != wordLength)){
        //     char letterGuess = letters.charAt(j);
        //     for(int i = 0; i < wordLength; i++){
        //         char wordChar = word.charAt(i);
        //         if (wordChar == letterGuess) correctLetter++;
        //         else didLose = true;

        //     }

        //     if (didLose) loses++;

        //     didLose = false;
        //     j++;
        // }
       

        // System.out.println("Correct letter int " + correctLetter);
        // if(correctLetter == wordLength) System.out.println("WIN");
        // else System.out.println("LOSE"); 
	}

}