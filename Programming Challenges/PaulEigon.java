import java.util.*;
import java.io.*;
public class PaulEigon{
    public static void main(String[] args) {
        Scanner keyboard = new Scanner(new BufferedReader(new InputStreamReader(System.in)));
        int numServes = keyboard.nextInt(), paulScore = keyboard.nextInt(), oppScore = keyboard.nextInt(), total = paulScore+oppScore;
        if(((total + 1) %2 == 0))

        //NOT FINISHED

        // for(int i = numServes; i < total+1;i++){
        //     if(i%numServes == 0){inc++;}
        // }

        // if(inc %2==0) {System.out.println("opponent");}
        // else{System.out.println("paul");}
        
    }
}